/**
 * Version constants — change to bump the build's reported version.
 * Used by the version switcher (VersionBadge) and shown in the UI.
 */
export const APP_VERSION = '2.0.0';
export const APP_CODENAME = 'chatbot';
